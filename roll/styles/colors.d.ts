export default colors;
declare namespace colors {
    const brand500: string;
    const brand700: string;
    const fg100: string;
    const fg500: string;
    const pending500: string;
    const pending700: string;
    const success500: string;
    const success700: string;
    const expired500: string;
    const expired700: string;
    const bg100: string;
}
